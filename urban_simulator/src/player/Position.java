package player;

import data.Particulars;

public class Position {
	
	protected Particulars particular;
	
	public void setPosition(Particulars person) {
		
		if (((person.getX()) >= 12) &&((person.getX()) <= 12)&&((person.getY()) >= 12)&&((person.getY()) <= 12)){
			setPlace();
		}
		if (((person.) >= 12) &&((person.getX()) <= 12)&&((person.getY()) >= 12)&&((person.getY()) <= 12))
	}
	

	
	
	
	
}
